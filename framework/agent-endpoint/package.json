{
  "name": "theokit-example-framework-agent-endpoint",
  "private": true,
  "type": "module",
  "description": "An agent is a file, and the file's path is its HTTP route.",
  "scripts": {
    "dev": "theokit dev",
    "build": "theokit build",
    "start": "theokit dev",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test --test-concurrency=1 tests/*.test.ts"
  },
  "dependencies": {
    "@theokit/agents": "12.1.0",
    "@theokit/sdk": "4.63.1",
    "theokit": "0.64.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
