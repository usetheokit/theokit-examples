/**
 * The framework requires an `app/` directory. This example is about what the agent remembers, not
 * about what a browser renders, so the page is the smallest thing that satisfies the requirement.
 */
export default function Page() {
  return <p>The remembering agent is at POST /api/agents/remember.</p>;
}
