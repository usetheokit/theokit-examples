{
  "name": "theokit-example-framework-claude-code-memory",
  "private": true,
  "type": "module",
  "description": "Does a SERVED agent write memory the Claude Code CLI can classify?",
  "scripts": {
    "dev": "theokit dev",
    "build": "theokit build",
    "start": "theokit dev",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test --test-concurrency=1 tests/*.test.ts"
  },
  "dependencies": {
    "@theokit/agents": "12.1.0",
    "@theokit/sdk": "4.63.1",
    "theokit": "0.63.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
